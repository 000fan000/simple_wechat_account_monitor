wx:
  token_file: "data/wx_token.json"

gather:
  timeout: 30
  interval: 3
  clean_html: false

proxy:
  enabled: false
  http: ""
  https: ""

server:
  host: "0.0.0.0"
  port: 8000
  debug: false

db:
  connection: "sqlite:///data/articles.db"